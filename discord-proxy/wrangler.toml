name = "discord-webhook-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# PROXY_KEY is NOT set here - it is a secret. Set it with:
#   wrangler secret put PROXY_KEY
# and use the same value as the backend's DISCORD_PROXY_SECRET env var.
