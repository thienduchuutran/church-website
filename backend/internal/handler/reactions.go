package handler

// ReactionHandler is a placeholder until reaction HTTP routes are implemented.
type ReactionHandler struct{}

func NewReactionHandler() *ReactionHandler {
	return &ReactionHandler{}
}
